export interface INavLinks { 
  text: string;
  href: string;
  controls: string;
}

export interface ISocialButton {
  arialLAbel: string;
  url: string;
  iconClass: string;
  index: number;
}

export interface IProjectImages {
  url: string;
  alt: string;
}